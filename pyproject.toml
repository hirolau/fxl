[project]
name = "fxl"
version = "0.1.0"
description = "Fill Excel templates using named cells and tables.s"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
